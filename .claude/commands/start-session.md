---
description: Begin a new working session by loading project memory
---

Start a new session on this project.

1. Read `C:\Users\User\.claude\projects\d--My-Backups-Bojan-system-pkgs-checker\memory\MEMORY.md` (the canonical memory index) and every memory file it points to.
2. If the project-local `memory/` folder (inside this working directory) is out of sync with the canonical one (different content or missing files), copy the canonical files over it so the local copy matches before continuing.
3. Give the user a short (3-6 sentence) recap: what this project is, what's been built, any pending/unresolved asks, and any standing feedback rules to follow this session.
4. Do not start implementation work yet — just load context and summarize, then wait for the user's request.
